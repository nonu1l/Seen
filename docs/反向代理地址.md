# 公开反向代理地址

如果你暂时不想自己部署 Cloudflare Worker，可以先使用这个反向代理地址：

```text
https://seen.slimrip.com
```

启动后进入设置页，在“搜索与数据源”里填写：

```text
https://seen.slimrip.com
```

这个地址用于代理 Bangumi API、Bangumi 图片 CDN 和 DuckDuckGo Lite 搜索。建议长期使用时仍然部署自己的 Cloudflare Worker 或绑定自己的域名，避免公共地址调整后影响使用。
